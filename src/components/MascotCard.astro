---
import { site } from '../data/site';

type Props = {
  alt: string;
  iconAlt?: string;
  image?: string;
  compact?: boolean;
};

const { alt, iconAlt = '', image = site.mascot, compact = false } = Astro.props;
---
<aside class:list={['mascot-card', { compact }]}>
  <img class="mascot-icon" src={site.icon} alt={iconAlt} width="112" height="112" />
  <img class="mascot-sheet" src={image} alt={alt} loading={compact ? 'lazy' : 'eager'} />
</aside>
